# NextHire – Campus Hiring Portal

# Note
use the demo code to verify the project not original one because it has database connection code which runs locally.

## 📌 Overview
NextHire is a Java-based desktop application designed to streamline campus recruitment.  
It provides secure authentication, centralized job listings, and admin oversight for placement cells.  
The project is built using **Java Swing (GUI)**, **JDBC**, and **MySQL/SQLite**.

---

## ⚙️ Compilation & Execution

To compile the code, run:

```bash
javac -d src src/job/management/system/*.java

To execute the program, run:
java -cp src job.management.system.Main


🔑 Admin Access
Inside the code, you can access the Admin Page using the following credentials:
- Email: Admin@gmail.com
- Password: 123




